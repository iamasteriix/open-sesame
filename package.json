{
  "name": "open-sesame",
  "private": false,
  "scripts": {
    "dev:server": "doppler run -- docker compose up -d server",
    "dev:build": "doppler run -- docker compose build"
  },
  "workspaces": [
    "apps/web",
    "apps/server"
  ]
}